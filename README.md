📚 Book Library (Django MVT Project)

A simple web application built with Django that allows users to manage a collection of books. This project demonstrates the use of Django MVT architecture, models, views, templates, and basic CRUD operations.
